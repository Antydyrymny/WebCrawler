{
    "functions": {
        "api/*": {
            "runtime": "node@16",
            "memory": 512
        }
    },
    "routes": [
        { "src": "/api/(.*)", "dest": "api/$1" },
        { "handle": "filesystem" },
        { "src": "/(.*)", "dest": "/index.html" }
    ]
}
